<%= form_for(chart) do |f| %>
  <% if chart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chart.errors.count, "error") %> prohibited this chart from being saved:</h2>

      <ul>
      <% chart.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fable_id %>
    <%= f.text_field :fable_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :reference_date %>
    <%= f.date_select :reference_date %>
  </div>

  <div class="field">
    <%= f.label :point %>
    <%= f.number_field :point %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
